packages:
  - "core"
  - "react-core"
  - "react"
  - "react-native"
  - "vue"
  - "svelte"
  - "elements"

# esbuild (via tsup) needs its postinstall to fetch the platform binary.
allowBuilds:
  esbuild: true
